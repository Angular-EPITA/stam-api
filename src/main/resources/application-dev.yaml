spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/stam_db
    username: stam_admin
    password: stam_password

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
